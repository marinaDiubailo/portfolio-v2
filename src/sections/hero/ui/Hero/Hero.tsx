import { AuroraBackground } from '@/shared/ui';
import s from './Hero.module.scss';

export const Hero = () => {
  return (
    <AuroraBackground>
      <section className={s.root}>Hero</section>
    </AuroraBackground>
  );
};
